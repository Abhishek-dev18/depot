# Copy to .env.turn and fill in before running:
#   docker compose --env-file .env.turn up -d

# A domain or host you control that resolves to this machine.
TURN_REALM=turn.example.com

# Long random secret used to derive time-limited TURN credentials.
# Generate one with: openssl rand -hex 32
TURN_SECRET=
